spring.jpa.open-in-view=false
spring.jpa.hibernate.ddl-auto=validate

spring.datasource.url=jdbc:h2:mem:testdb
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2

spring.h2.console.settings.trace=false
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

logging.level.com.vapitea.datacollector=debug
logging.level.org.springframework.security=debug

# TLS config
# The format used for the keystore. It could be set to JKS in case it is a JKS file
server.ssl.key-store-type=PKCS12
# The path to the keystore containing the certificate
server.ssl.key-store=classpath:keystore/data-collector.p12
# The password used to generate the certificate
server.ssl.key-store-password=narancs
# The alias mapped to the certificate
server.ssl.key-alias=data-collector-key
# Allow only tls
server.ssl.enabled=false
# End of TLS config

#TLS config for RestTemplate

#End of TLS config for RestTemplate
